import { useState, useEffect } from "react"

const Mains = () => {

    const [mains, setMains] = useState([])

    useEffect((), [])

    return (
        <div>

        </div>
    );
};

export default Mains;